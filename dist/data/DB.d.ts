export declare let DB: {
    posts: {
        id: `${string}-${string}-${string}-${string}-${string}`;
        title: string;
        shortDescription: string;
        content: string;
        blogId: string;
        blogName: string;
    }[];
    blogs: {
        id: `${string}-${string}-${string}-${string}-${string}`;
        name: string;
        description: string;
        websiteUrl: string;
    }[];
};
